---
paths:
  - "**/*.go"
  - "tools/**"
---
- Go は `_*` / `.*` で始まるファイルを build から除外する仕様。lint や go vet の検証用ダミーは `zz_*.go` 等の通常ファイル名で置く。lint が空振りしたら `go list -f '{{.GoFiles}}' ./<pkg>` で対象 .go が列挙されているか確認し、列挙されていなければファイル名側を疑う。
