# 本テンプレは観点カテゴリの「種 (枠)」であり、`name` / `description` / `provider` / `model` の 4 フィールドのみを保持する。
# `applies_when` / `prompt` は出荷状態では持たず、Phase 4 で `references/aspects/config-exposure.md` の合成ガイドに従い、
# Phase 1 のリポジトリ調査結果を織り込んで合成される (出荷状態の「コピペ即動作」要件は撤回)。
# provider / model は利用環境に応じて codex / cursor / cursor-sdk に変更可。
name: config-exposure-review
description: 設定露出観点 (env / config / secrets コミット混入 / hard-coded credential / 暗号化 / 公開境界)
provider: claude
model: claude-opus-4-7
